interface:
  display_name: "Eliza"
  short_description: "Repo-specific patterns and workflows for eliza"
  default_prompt: "Use the eliza repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true